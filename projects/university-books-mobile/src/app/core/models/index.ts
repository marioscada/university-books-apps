/**
 * Domain Models Barrel Export
 *
 * Domain models represent business entities with logic, validation, and computed properties.
 * They are different from API types (generated/models/*) which are raw data structures.
 *
 * Use:
 * - Domain Models: For business logic in services and components
 * - API Types: For HTTP requests/responses
 */

export * from './document.model';
